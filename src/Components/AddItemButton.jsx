import "../Styles/addItemButton.css"
const AddItemButton = ({ data }) => {
  const saveData = async () => {

  }
  return (
    <div className="save-btn-container">
      <button className="save-btn" onClick={() => saveData}>Save data</button>
      </div>

  )

}

export default AddItemButton